const ACTIVE = "ACTIVE";
const ON_HOLD = "ON_HOLD";
const INACTIVE = "INACTIVE";

const ALL_STATUS = [ACTIVE, ON_HOLD, INACTIVE];
const STATUS_TYPES = {
  ACTIVE,
  ON_HOLD,
  INACTIVE,
};

module.exports = {
  STATUS_TYPES,
  ALL_STATUS,
};
